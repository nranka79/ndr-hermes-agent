[phases.setup]
nixPkgs = ["...", "git"]

[phases.install]
cmds = [
  "npm install",
  "npm install -g @googleworkspace/cli",
  "pip install -e ."
]

[start]
cmd = "python3 setup_oauth_credentials.py && exec hermes gateway"
