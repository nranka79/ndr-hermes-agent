# Vinod Kumar Das — Brain Dream Run (Jul 6 2026)

## Context

- **Cron target**: Old Telegram ID `vkdas` (live stale dir, no brain — only `.bun/` cache)
- **DRAAS account**: `vkdas` at `/data/hermes/users/vkdas/`
- **Brain**: `brain-copy/` (hermes-owned, PGLite working, git history intact — 2 commits)
- **Config**: `.gbrain-writable/` with `minimax:embo-01`
- **Bun**: `/data/hermes/home/.local/bin/bun` (Tier 0.5 — pre-installed, works)
- **Invocation method**: `GBRAIN_HOME` (preferred — no HOME redirect)

## Key Discovery

### safe.directory stale pointer

The system gitconfig had:
```
/data/hermes/users/vkdas/brain-copy  (stale — this dir no longer exists)
```

But was **missing**:
```
/data/hermes/users/vkdas/brain-copy  (the actual target)
```

When using `GBRAIN_HOME` (which keeps HOME at system default and reads `/data/hermes/home/.gitconfig`), git rejected the brain-copy dir with `fatal: detected dubious ownership` because the whitelisted path was stale.

**Fix**: `git config --global --add safe.directory /data/hermes/users/vkdas/brain-copy`

This is now encoded in the AUTOMATIC FALLBACK section of gbrain-ops SKILL.md — after resolving a fallback path, always check+add safe.directory before running dream.

## Results

- **Status**: `partial` (expected — embed failed)
- **Duration**: 797ms
- **Pages imported**: 1 (identity.md — default scaffolding)
- **Embed**: Failed — `minimax:embo-01 requires MINIMAX_API_KEY`
- **Phases ok**: 16/20 (all DB phases except embed skipped)
- **Notable**: Full reimport triggered due to missing anchor commit (previous history rewrite)
- **Safe.directory fix needed**: Yes — added `/data/hermes/users/vkdas/brain-copy`
