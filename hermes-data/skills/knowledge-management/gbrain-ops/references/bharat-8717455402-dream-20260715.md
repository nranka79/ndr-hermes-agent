# Bharat Hawaldar — gbrain dream 2026-07-15

## Context

- **Cron target**: `uid=[REDACTED-TID]` (numeric Telegram ID)
- **DRAAS account**: `sales1.blr`
- **Stale dir**: `/data/hermes/users/[REDACTED-TID]/` exists with `.gbrain/`, `.bun/`, `.npm/` but **no brain dir**
- **Brain**: `/data/hermes/users/sales1.blr/brain-copy` (hermes-owned, git repo with 2 commits)
- **Config**: `/data/hermes/users/sales1.blr/.gbrain-writable/.gbrain/config.json` — PGLite, `minimax:embo-01`
- **Bun**: `/data/hermes/home/.local/bin/bun`
- **Invocation**: `GBRAIN_HOME=/data/hermes/users/sales1.blr/.gbrain-writable /data/hermes/home/.local/bin/bun /opt/gbrain/src/cli.ts dream --json --dir /data/hermes/users/sales1.blr/brain-copy`

## Results

- **Status**: `partial` (expected — embed failed)
- **Duration**: 586ms
- **Phases**: 22 phases, all DB-backed phases OK except embed

### Phase Highlights

| Phase | Status | Detail |
|---|---|---|
| sync | ✅ ok | +1 page imported (full reimport — git anchor gc'd from prior setup cycle) |
| embed | ❌ fail | `minimax:embo-01 requires MINIMAX_API_KEY` |
| propose_takes | ✅ ok | 7 pages scanned, 0 proposals (all 7 LLM extractors failed: missing ANTHROPIC_API_KEY) |
| calibration_profile | ✅ ok | `holder=garry`, 0 resolved takes (insufficient data) |
| orphans | ⚠️ warn | 7 orphans out of 7 total (default scaffold fingerprint) |
| lint | ⚠️ warn | 1 issue remaining on identity.md |
| consolidate | ✅ ok | 0 facts promoted |

### Key Observations

1. **Brain-copy git anchor gc'd**: `Sync anchor 4dfd3f9d object missing (gc'd after history rewrite). Running full reimport.` — brain-copy was not replaced; the anchor was garbage-collected from a prior `rm -rf .git && git init` cycle. Harmless reimport of 1 page (~256ms).
2. **Default scaffold fingerprint confirmed**: 7 phantom pages (scaffold templates), `holder=garry`, all zero totals except orphans. Brain-copy has only `identity.md` (1 real file).
3. **GBRAIN_HOME + brain-copy worked cleanly**: No git config breakage, no `safe.directory` issues, no `HOME` redirect needed.
4. **Numeric Telegram ID `[REDACTED-TID]` stale path**: Has `.gbrain/`, `.bun/`, `.npm/` but no `brain/` — cron targeting this uid must fall back to `sales1.blr`.
