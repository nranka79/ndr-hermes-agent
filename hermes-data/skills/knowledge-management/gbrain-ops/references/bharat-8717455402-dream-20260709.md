# Bharat Hawaldar — gbrain dream (Jul 9 2026 Run)

**Session**: Cron job — `gbrain dream --json` for uid=sales1.blr

## Discovery: Telegram ID path has a working Postgres brain

Contrary to earlier belief, the stale Telegram ID path `/data/hermes/users/sales1.blr/` has a **working Postgres/Supabase gbrain** at the default `.gbrain/` config path:

- `.gbrain/` exists, is hermes-owned, contains `audit/` subdirectory
- `gbrain doctor --json` confirms: `schema_version: 2`, Postgres engine, 7 pages
- No local `brain/` directory — all content lives in Postgres DB (not PGLite)
- Dream runs **without `--dir`** — DB-only phases work, filesystem phases skip gracefully

## Command

```sh
HOME=/data/hermes/users/sales1.blr bun run /opt/gbrain/src/cli.ts dream --json
```

## Results

| Metric | Value |
|--------|-------|
| Duration | 433ms |
| Status | `partial` |
| Engine | Postgres (Supabase) |
| Pages | 7 (default scaffolding) |

## Phase summary

| Phase | Status | Detail |
|-------|--------|--------|
| lint | ⚠️ warn | 1 issue remaining |
| backlinks | ✅ ok | 0 gaps |
| sync | ✅ ok | 0 added, up to date |
| synthesize | ⏭️ skipped | not configured |
| extract | ✅ ok | 0 links |
| extract_facts | ✅ ok | 0 facts |
| extract_atoms | ⏭️ skipped | pack-gated |
| resolve_symbol_edges | ✅ ok | 0 chunks |
| patterns | ⏭️ skipped | insufficient evidence |
| synthesize_concepts | ⏭️ skipped | pack-gated |
| recompute_emotional_weight | ✅ ok | 0 pages |
| consolidate | ✅ ok | 0 facts promoted |
| propose_takes | ✅ ok | 7 scanned, 0 proposals |
| grade_takes | ✅ ok | 0 graded |
| calibration_profile | ⏭️ skipped | insufficient data |
| conversation_facts_backfill | ⏭️ skipped | disabled |
| enrich_thin | ⏭️ skipped | disabled |
| skillopt | ⏭️ skipped | disabled |
| embed | ❌ fail | needs ZEROENTROPY_API_KEY |
| orphans | ⚠️ warn | 7/7 orphans |
| schema-suggest | ✅ ok | 0 suggestions |
| purge | ✅ ok | nothing to purge |

## Key observations

1. **Postgres brain at Telegram ID path** — the `.gbrain/` config at the old numeric path was never cleaned up during migration and continues to function. It connects to Supabase (shared Postgres for multi-user gbrain).
2. **No local brain dir** — `--dir` flag fails with "path does not exist" because there's no `brain/` directory. Dream works fine without it.
3. **Embed fails** — configured model is `zeroentropyai:zembed-1` which needs `ZEROENTROPY_API_KEY`. No such env var is set.
4. **Propose_takes warns** — 7 extractor failures due to missing `ANTHROPIC_API_KEY` (harmless, graceful skip).
5. **brain_dir in output** reads `/data/hermes/users/psingh/brain` — this is a default from the config and does not reflect the actual data source (Postgres).

## Relevance to cron

This means the Telegram ID path `sales1.blr` IS a functional brain path for dream (Postgres-backed), but the DRAAS account path `sales1.blr` is the canonical one going forward. The Step 0 fallback in the cron pattern (detecting no `brain/` dir) correctly triggers the fallback to `sales1.blr`, but both paths work for dream independently.
